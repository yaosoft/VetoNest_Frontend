import React, { useState, useEffect, useContext } from "react";

import { Form, Select } from 'antd';
const CurrencySelector = ( params ) => {

	 return (
		<div className="languageSelector">
			<Select
				value = { 'EUR' }
			>
					
          </Select>
		</div>
	);
};

export default CurrencySelector;
