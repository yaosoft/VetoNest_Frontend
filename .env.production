# Production API endpoints (loaded automatically by `npm run build`).
# Values migrated verbatim from the previously active production config
# (src/config/api.js production comment + the live lines in src/context/site.js).
REACT_APP_API_BASE_URL=https://backend.vetonest.com/api/
REACT_APP_BASE_URL=https://backend.vetonest.com/
